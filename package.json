{
  "name": "trump-hearts-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:all": "npm run install:all && cd frontend && npm run build && cd ../backend && npx prisma generate && npx prisma db push",
    "start": "cd backend && node index.js"
  }
}
